# Desktop AI Pet - 프로젝트 기획서

**작성일:** 2026-01-08  
**버전:** v0.2.0 (계획)  
**기술 스택:** Tauri + React + TypeScript + Rust

---

## 1. 프로젝트 개요

### 1.1 비전

> "개발자의 바탕화면에 항상 떠 있는 AI 동반자"

단순한 챗봇이 아닌, **로컬 환경을 인식하고 능동적으로 도움을 주는** 데스크톱 펫

### 1.2 왜 데스크톱 펫인가?

| ChatGPT/Claude 웹     | 데스크톱 펫          |
| --------------------- | -------------------- |
| 브라우저 탭 전환 필요 | 항상 화면에 존재     |
| 컨텍스트 매번 설명    | 프로젝트 자동 인식   |
| 파일 접근 불가        | 로컬 파일시스템 접근 |
| 터미널 접근 불가      | 빌드/테스트 모니터링 |
| 수동적 (질문 대기)    | 능동적 (알림, 제안)  |

### 1.3 타겟 사용자

- **Primary**: 개발자 (특히 VSCode/터미널 헤비 유저)
- **Secondary**: AI 도구에 관심 있는 파워 유저

### 1.4 차별화 포인트 (USP)

```
┌─────────────────────────────────────────────────────────────┐
│                    차별화 전략                              │
├─────────────────────────────────────────────────────────────┤
│  1. 개발자 특화 기능 (에러 분석, Git 알림, 빌드 모니터링)  │
│  2. MCP 기반 확장성 (filesystem, git, web-search)          │
│  3. AI 오케스트레이션 (멀티 에이전트, 작업 라우팅)         │
│  4. 가벼움 + 프라이버시 (Ollama 로컬 우선)                 │
│  5. 고성능 (Tauri - 번들 10배 감소, 메모리 4배 감소)       │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 경쟁 분석

### 2.1 주요 경쟁 프로젝트

| 프로젝트                     | 스택      | 강점                         | 약점                   |
| ---------------------------- | --------- | ---------------------------- | ---------------------- |
| **Airi** (16.6k⭐)           | Tauri+Vue | Live2D, 음성, 게임 연동, MCP | 복잡함, 리소스 많이 씀 |
| **BongoCat** (15.8k⭐)       | Tauri     | 가벼움, 키보드 반응          | AI 없음                |
| **Open-LLM-VTuber** (5.5k⭐) | Python    | 로컬 LLM, 음성               | 데스크톱 앱 아님       |

### 2.2 Airi 기술 분석

Airi는 **Tauri + Vue 3** 기반:

- **Live2D/VRM** 캐릭터 렌더링 (감정 표현, 립싱크)
- **MCP 연동** (`tauri-plugin-mcp` - Rust 플러그인)
- **30+ LLM 프로바이더** 지원
- **음성 파이프라인** (STT → LLM → TTS)
- **게임 에이전트** (마인크래프트, 팩토리오)

**우리가 모방할 것:**

- Tauri 아키텍처
- MCP 플러그인 패턴
- 멀티 LLM 프로바이더

**우리가 차별화할 것:**

- 개발자 특화 기능
- 더 가벼운 구현
- 음성 제외 (텍스트 집중)

---

## 3. 왜 Tauri인가?

### 3.1 Electron vs Tauri 비교

| 항목        | Electron      | Tauri           |
| ----------- | ------------- | --------------- |
| 번들 크기   | ~150MB        | **~3-10MB**     |
| 메모리 사용 | ~150-300MB    | **~30-80MB**    |
| 시작 시간   | 2-5초         | **<1초**        |
| 백엔드 언어 | Node.js       | **Rust**        |
| 프론트엔드  | Chromium 내장 | 시스템 WebView  |
| 보안        | 중간          | **높음** (Rust) |

### 3.2 Tauri 장점

```
┌─────────────────────────────────────────────────────────────┐
│  성능 (Performance)                                         │
│  ├── 번들 크기 95% 감소 (150MB → 8MB)                       │
│  ├── 메모리 75% 감소 (200MB → 50MB)                         │
│  └── 시작 시간 80% 감소 (3초 → 0.5초)                       │
│                                                             │
│  보안 (Security)                                            │
│  ├── Rust 메모리 안전성                                     │
│  ├── 세밀한 권한 제어 (capabilities)                        │
│  └── IPC 보안 강화                                          │
│                                                             │
│  생태계 (Ecosystem)                                         │
│  ├── Airi의 MCP 플러그인 재사용 가능                        │
│  ├── tauri-plugin-mcp (검증됨)                              │
│  └── Rust 크레이트 활용 가능                                │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 마이그레이션 난이도

| 영역               | 난이도    | 이유                          |
| ------------------ | --------- | ----------------------------- |
| 프론트엔드 (React) | 🟢 쉬움   | 거의 변경 없음                |
| IPC 통신           | 🟡 중간   | API 형태 다름 (`invoke` 사용) |
| 시스템 API         | 🟡 중간   | Rust로 재작성                 |
| 투명 창/클릭 통과  | 🔴 어려움 | Rust 플러그인 필요            |

---

## 4. 외부 LLM 프로바이더 지원

### 4.1 지원 대상

| 프로바이더         | 모델 예시                      | 용도             | 비용 |
| ------------------ | ------------------------------ | ---------------- | ---- |
| **Ollama**         | llama3, codellama, mistral     | 로컬, 프라이버시 | 무료 |
| **OpenAI**         | gpt-4o, gpt-4o-mini            | 범용, 빠름       | 유료 |
| **Anthropic**      | claude-sonnet-4, claude-opus-4 | 코딩, 분석       | 유료 |
| **GitHub Copilot** | copilot-chat                   | 코드 특화        | 구독 |
| **Google**         | gemini-2.0-flash               | 빠른 응답        | 유료 |
| **OpenRouter**     | 다양한 모델                    | 통합 게이트웨이  | 유료 |

### 4.2 프로바이더 추상화 설계 (Rust)

```rust
// src-tauri/src/ai/provider.rs
use async_trait::async_trait;

#[async_trait]
pub trait AiProvider: Send + Sync {
    fn name(&self) -> &str;

    async fn chat(&self, messages: Vec<Message>) -> Result<String>;

    async fn stream(&self, messages: Vec<Message>) -> Result<impl Stream<Item = String>>;

    fn supports_tools(&self) -> bool;

    async fn call_with_tools(
        &self,
        messages: Vec<Message>,
        tools: Vec<Tool>
    ) -> Result<ToolCallResult>;
}
```

### 4.3 라우팅 규칙

| 작업 유형       | 판단 기준                 | 사용 모델            |
| --------------- | ------------------------- | -------------------- |
| **일반 대화**   | 기본값                    | Ollama (llama3)      |
| **코드 작성**   | 코드 블록 요청, 파일 수정 | Claude Sonnet        |
| **빠른 검색**   | 단순 질문, 사실 확인      | GPT-4o-mini          |
| **복잡한 분석** | 아키텍처, 디버깅          | GPT-4o / Claude Opus |

### 4.4 설정 UI

```
┌─────────────────────────────────────────────────────────────┐
│  AI 프로바이더 설정                                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [기본 프로바이더]                                          │
│  ┌─────────────────────────────────────┐                   │
│  │ ◉ Ollama (로컬)                     │ ← 무료, 프라이버시│
│  │ ○ OpenAI                            │                   │
│  │ ○ Anthropic                         │                   │
│  │ ○ GitHub Copilot                    │                   │
│  └─────────────────────────────────────┘                   │
│                                                             │
│  [API 키 설정]                                              │
│  OpenAI API Key:     [sk-••••••••••••••••] [저장]          │
│  Anthropic API Key:  [sk-ant-••••••••••••] [저장]          │
│                                                             │
│  [고급 설정]                                                │
│  ☑ 코드 작업은 Claude 사용                                 │
│  ☑ 빠른 검색은 GPT-4o-mini 사용                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. MCP (Model Context Protocol) 연동

### 5.1 연동 대상 MCP 서버

| MCP 서버       | 기능                | 사용 시나리오                        |
| -------------- | ------------------- | ------------------------------------ |
| **filesystem** | 파일 읽기/쓰기/검색 | "package.json 보여줘"                |
| **git**        | 상태, diff, 커밋    | "뭐 바꿨어?", "커밋 메시지 만들어줘" |
| **web-search** | 웹 검색             | "React 19 새 기능 뭐야?"             |
| **context7**   | 공식 문서 검색      | "Next.js App Router 사용법"          |

### 5.2 Airi MCP 구현 참고

Airi는 `tauri-plugin-mcp` (Rust)로 MCP 구현:

```rust
// Airi의 구현 (crates/tauri-plugin-mcp/src/lib.rs)
use rmcp::{
  model::{CallToolRequestParam, CallToolResult, Tool},
  service::RunningService,
  transport::TokioChildProcess,
  RoleClient,
  ServiceExt,
};

#[tauri::command]
async fn connect_server(command: String, args: Vec<String>) -> Result<(), String> {
  let child_process = TokioChildProcess::new(
    Command::new(command).args(args)
  ).unwrap();

  let service: RunningService<RoleClient, ()> = ().serve(child_process).await.unwrap();
  // ...
}

#[tauri::command]
async fn call_tool(name: String, args: Map<String, Value>) -> Result<CallToolResult, String> {
  // MCP 도구 호출
}
```

**우리도 동일 패턴 사용 예정**

---

## 6. 개발자 특화 기능

### 6.1 클립보드 에러 감지

**시나리오:**

```
[터미널에서 에러 복사]
     ↓
[펫이 감지]
     ↓
🐱 "이 에러 분석해줄까?" [예] [아니오]
     ↓
🐱 "TypeError: Cannot read property 'map' of undefined
    → data가 undefined일 때 .map() 호출한 것 같아."
```

### 6.2 Git 상태 알림

**시나리오:**

```
[6시간 동안 커밋 없음 + 변경사항 있음]
     ↓
🐱 "12개 파일 수정했는데 커밋 안 했어. 잊은 거 아니야?"
    [커밋 메시지 생성] [나중에] [오늘 알림 끄기]
```

### 6.3 프로젝트 컨텍스트 자동 인식

**시나리오:**

```
[VS Code에서 ~/projects/my-app 열림]
     ↓
[펫이 감지: package.json → Next.js 프로젝트]
     ↓
🐱 컨텍스트 자동 설정: "Next.js 14, TypeScript, Tailwind"
     ↓
[질문: "라우팅 어떻게 해?"]
     ↓
🐱 "App Router 쓰고 있네. app/ 폴더에 page.tsx 만들면 돼..."
```

---

## 7. 아키텍처 (목표)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Desktop AI Pet v1.0                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │Pet Window│  │Chat Win  │  │Caption   │  │Settings  │        │
│  │(Live2D)  │  │(Bubble)  │  │(말풍선)  │  │(설정 UI) │        │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘        │
│       └─────────────┴─────────────┴─────────────┘               │
│                           │ IPC                                  │
│  ┌────────────────────────┴────────────────────────────────┐    │
│  │                    Tauri Main (Rust)                     │    │
│  │                                                          │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │              AI Orchestrator                     │    │    │
│  │  │                                                  │    │    │
│  │  │  ┌──────────┐   ┌──────────┐   ┌──────────┐    │    │    │
│  │  │  │ Classify │ → │  Route   │ → │ Execute  │    │    │    │
│  │  │  └──────────┘   └──────────┘   └──────────┘    │    │    │
│  │  │                                                  │    │    │
│  │  │  Providers:                                      │    │    │
│  │  │  ┌────────┐ ┌────────┐ ┌────────┐              │    │    │
│  │  │  │ Ollama │ │ OpenAI │ │Anthropic│              │    │    │
│  │  │  └────────┘ └────────┘ └────────┘              │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                                          │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │              MCP Clients                         │    │    │
│  │  │  ┌────────────┐ ┌─────┐ ┌───────────┐          │    │    │
│  │  │  │ filesystem │ │ git │ │web-search │          │    │    │
│  │  │  └────────────┘ └─────┘ └───────────┘          │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                                          │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │              System Monitors                     │    │    │
│  │  │  ┌───────────┐ ┌─────────┐ ┌──────────┐        │    │    │
│  │  │  │ Clipboard │ │ Git     │ │ Terminal │        │    │    │
│  │  │  └───────────┘ └─────────┘ └──────────┘        │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 8. 프로젝트 구조 (목표)

```
my-ai-pet/
├── src/                          # 프론트엔드 (React)
│   ├── assets/
│   ├── components/
│   ├── hooks/
│   ├── windows/
│   │   ├── pet/                  # 펫 창
│   │   ├── chat/                 # 채팅 창
│   │   ├── caption/              # 캡션 창
│   │   └── settings/             # 설정 창
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/                    # 백엔드 (Rust)
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── commands/             # Tauri 커맨드
│   │   │   ├── mod.rs
│   │   │   ├── window.rs         # 창 관리
│   │   │   ├── chat.rs           # 채팅 IPC
│   │   │   └── settings.rs       # 설정
│   │   ├── ai/                   # AI 프로바이더
│   │   │   ├── mod.rs
│   │   │   ├── provider.rs       # 프로바이더 trait
│   │   │   ├── router.rs         # 라우터
│   │   │   ├── ollama.rs
│   │   │   ├── openai.rs
│   │   │   └── anthropic.rs
│   │   ├── mcp/                  # MCP 연동
│   │   │   ├── mod.rs
│   │   │   └── client.rs
│   │   ├── monitors/             # 시스템 모니터
│   │   │   ├── mod.rs
│   │   │   ├── clipboard.rs
│   │   │   └── git.rs
│   │   └── db/                   # 데이터베이스
│   │       └── mod.rs
│   ├── Cargo.toml
│   └── tauri.conf.json
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 9. 기술 스택

### 9.1 Cargo.toml 의존성 (예정)

```toml
[package]
name = "my-ai-pet"
version = "0.2.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0", features = ["tray-icon", "macos-private-api"] }
tauri-plugin-store = "2.0"
tauri-plugin-clipboard-manager = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "stream"] }
async-trait = "0.1"
futures = "0.3"

# AI Providers
async-openai = "0.23"

# MCP (Airi 참고)
rmcp = { version = "0.1", features = ["client", "transport-child-process"] }

# Database
rusqlite = { version = "0.31", features = ["bundled"] }
```

### 9.2 package.json 의존성 (예정)

```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
```

---

## 10. 구현 로드맵

### Phase 0: Tauri 마이그레이션 (2주)

| 작업 | 설명                         | 예상 시간 |
| ---- | ---------------------------- | --------- |
| 0-1  | Tauri 프로젝트 초기화        | 0.5일     |
| 0-2  | React 프론트엔드 이전        | 1일       |
| 0-3  | IPC 통신 재작성 (Rust)       | 2일       |
| 0-4  | 투명 창 + 클릭 통과 구현     | 3일       |
| 0-5  | 창 관리 (pet, chat) 재구현   | 2일       |
| 0-6  | 시스템 트레이 재구현         | 1일       |
| 0-7  | 기존 기능 테스트 + 버그 수정 | 2일       |

**산출물:** Electron과 동일한 기능의 Tauri 앱

### Phase 1: 멀티 LLM 프로바이더 (1.5주)

| 작업 | 설명                              | 예상 시간 |
| ---- | --------------------------------- | --------- |
| 1-1  | 프로바이더 추상화 (Rust trait)    | 1일       |
| 1-2  | Ollama 프로바이더                 | 1일       |
| 1-3  | OpenAI 프로바이더                 | 1일       |
| 1-4  | Anthropic 프로바이더              | 1일       |
| 1-5  | 스트리밍 응답 구현                | 2일       |
| 1-6  | 설정 UI (프로바이더 선택, API 키) | 2일       |
| 1-7  | 라우팅 규칙 구현                  | 1일       |

**산출물:** 다중 LLM 지원 + 스트리밍

### Phase 2: MCP 연동 (2주)

| 작업 | 설명                              | 예상 시간 |
| ---- | --------------------------------- | --------- |
| 2-1  | tauri-plugin-mcp 연동 (Airi 참고) | 2일       |
| 2-2  | filesystem 서버 연동              | 2일       |
| 2-3  | git 서버 연동                     | 2일       |
| 2-4  | web-search 서버 연동              | 2일       |
| 2-5  | 도구 호출 UI                      | 2일       |

**산출물:** MCP 기반 파일/Git/검색

### Phase 3: 캐릭터 & UX (1.5주)

| 작업 | 설명                    | 예상 시간 |
| ---- | ----------------------- | --------- |
| 3-1  | Live2D 또는 Lottie 통합 | 3일       |
| 3-2  | 모션 시스템             | 2일       |
| 3-3  | Caption 창              | 2일       |
| 3-4  | 스마트 포지셔닝         | 1일       |

**산출물:** 애니메이션 캐릭터 + 말풍선

### Phase 4: 개발자 기능 (1.5주)

| 작업 | 설명                        | 예상 시간 |
| ---- | --------------------------- | --------- |
| 4-1  | 클립보드 에러 감지          | 2일       |
| 4-2  | Git 상태 알림               | 2일       |
| 4-3  | 프로젝트 컨텍스트 자동 인식 | 2일       |
| 4-4  | 빌드/테스트 모니터링        | 2일       |

**산출물:** 능동적 알림 시스템

### 타임라인

```
Week 1-2:   [===== Phase 0: Tauri 마이그레이션 =====]
Week 3-4:   [==== Phase 1: 멀티 LLM ====]
Week 5-6:   [===== Phase 2: MCP 연동 =====]
Week 7-8:   [=== Phase 3: 캐릭터 ===][=== Phase 4: 개발자 기능 ===]
```

**총 예상 기간:** 8주 (2개월)

---

## 11. 성능 지표

### 11.1 성능 목표

| 지표         | Electron (현재) | Tauri (목표)          |
| ------------ | --------------- | --------------------- |
| 번들 크기    | ~150MB          | **<15MB**             |
| 메모리 사용  | ~200MB          | **<80MB**             |
| 시작 시간    | ~3초            | **<1초**              |
| 첫 응답 시간 | ~2초            | **<0.5초** (스트리밍) |

### 11.2 기능 목표

| 지표                | 목표     |
| ------------------- | -------- |
| 지원 LLM 프로바이더 | 5개 이상 |
| MCP 서버 연동       | 3개 이상 |
| 캐릭터 모션         | 5개 이상 |

---

## 12. 리스크 및 대응

| 리스크               | 영향 | 확률 | 대응                           |
| -------------------- | ---- | ---- | ------------------------------ |
| Live2D 라이선스 이슈 | 높음 | 중간 | 무료 모델 사용, Lottie 대체    |
| MCP 서버 안정성      | 중간 | 낮음 | 공식 서버 우선, 폴백 구현      |
| Tauri 러닝커브       | 중간 | 중간 | Rust 기초 학습, Airi 코드 참고 |
| API 비용 증가        | 중간 | 높음 | Ollama 우선, 사용량 제한       |

---

## 13. Debate 포인트

### 찬성 논거

1. **Tauri = 성능 + 보안**
   - Airi도 Tauri 사용 (검증됨)
   - 번들 크기 10배 감소
   - Rust 메모리 안전성

2. **멀티 프로바이더 = 유연성**
   - OpenCode처럼 사용자가 선택
   - 로컬 + 클라우드 하이브리드
   - 비용/성능/프라이버시 트레이드오프

3. **MCP = 확장성**
   - Airi의 tauri-plugin-mcp 재사용 가능
   - 표준 프로토콜 → 생태계 성장

4. **개발자 특화 = 차별화**
   - 범용 펫이 아닌 개발자 도구
   - 에러 감지, Git 알림 등 실용적 기능

### 반대 논거 (예상)

| 반론                        | 대응                         |
| --------------------------- | ---------------------------- |
| "Tauri 러닝커브 높다"       | Rust 기초만 필요, IPC는 단순 |
| "Electron 생태계가 더 크다" | 핵심 기능은 Tauri로 충분     |
| "마이그레이션 2주면 부족"   | 기존 코드 복잡하지 않음      |
| "API 키 관리 보안 이슈"     | Tauri secure storage 사용    |

---

## 14. 결론

```
┌─────────────────────────────────────────────────────────────┐
│                    핵심 전략                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Tauri 마이그레이션 (성능 최우선)                        │
│     → 번들 10배 감소, 메모리 4배 감소                       │
│                                                             │
│  2. 멀티 LLM 프로바이더 (유연성)                            │
│     → Ollama, OpenAI, Anthropic, Copilot 지원              │
│     → 사용자가 선택, 작업별 라우팅                         │
│                                                             │
│  3. MCP 연동 (확장성)                                       │
│     → Airi의 tauri-plugin-mcp 활용                         │
│                                                             │
│  4. 개발자 특화 기능 (차별화)                               │
│     → 에러 감지, Git 알림, 프로젝트 인식                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

타임라인: 8주 (Phase 0-4)
핵심 의존성: Tauri 2.0, rmcp, async-openai
```
